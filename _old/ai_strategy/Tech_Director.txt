get_to_work_shein_worker = {
	enable = {
		always = yes
	}
	abort_when_not_enabled = yes


	ai_strategy = {
		type = research_tech
		id = basic_machine_tools
		value = 50000  # Value doesn't really matter as long as its positive. The AI should be forced to research the specified technology ASAP (if able).
	}
	ai_strategy = {
		type = research_tech
		id = improved_machine_tools
		value = 50000  # Value doesn't really matter as long as its positive. The AI should be forced to research the specified technology ASAP (if able).
	}
	ai_strategy = {
		type = research_tech
		id = advanced_machine_tools
		value = 50000  # Value doesn't really matter as long as its positive. The AI should be forced to research the specified technology ASAP (if able).
	}
	ai_strategy = {
		type = research_tech
		id = flexible_line
		value = 50000  # Value doesn't really matter as long as its positive. The AI should be forced to research the specified technology ASAP (if able).
	}
}

doctrines = {
	enable = {
		always = yes
	}
	abort_when_not_enabled = yes


	ai_strategy = {
		type = research_weight_factor
		id = mobile_warfare
		value = 5000  # Value doesn't really matter as long as its positive. The AI should be forced to research the specified technology ASAP (if able).
	}
	ai_strategy = {
		type = research_weight_factor
		id = delay
		value = 5000  # Value doesn't really matter as long as its positive. The AI should be forced to research the specified technology ASAP (if able).
	}
	ai_strategy = {
		type = research_weight_factor
		id = elastic_defence
		value = 5000  # Value doesn't really matter as long as its positive. The AI should be forced to research the specified technology ASAP (if able).
	}
	ai_strategy = {
		type = research_weight_factor
		id = armored_spearhead
		value = 5000  # Value doesn't really matter as long as its positive. The AI should be forced to research the specified technology ASAP (if able).
	}
	ai_strategy = {
		type = research_weight_factor
		id = schwerpunk
		value = 5000  # Value doesn't really matter as long as its positive. The AI should be forced to research the specified technology ASAP (if able).
	}
	ai_strategy = {
		type = research_weight_factor
		id = blitzkrieg
		value = 5000  # Value doesn't really matter as long as its positive. The AI should be forced to research the specified technology ASAP (if able).
	}
	ai_strategy = {
		type = research_weight_factor
		id = kampfgruppe
		value = 5000  # Value doesn't really matter as long as its positive. The AI should be forced to research the specified technology ASAP (if able).
	}
	ai_strategy = {
		type = research_weight_factor
		id = firebrigades
		value = 5000  # Value doesn't really matter as long as its positive. The AI should be forced to research the specified technology ASAP (if able).
	}
	ai_strategy = {
		type = research_weight_factor
		id = backhand_blow
		value = 5000  # Value doesn't really matter as long as its positive. The AI should be forced to research the specified technology ASAP (if able).
	}
	ai_strategy = {
		type = research_weight_factor
		id = modern_blitzkrieg
		value = 5000  # Value doesn't really matter as long as its positive. The AI should be forced to research the specified technology ASAP (if able).
	}
}

build_like_china_not_like_america = {
	enable = {
		always = yes
	}
	abort_when_not_enabled = yes


	ai_strategy = {
		type = research_tech
		id = construction1
		value = 50000  # Value doesn't really matter as long as its positive. The AI should be forced to research the specified technology ASAP (if able).
	}
	ai_strategy = {
		type = research_tech
		id = construction1
		value = 50000  # Value doesn't really matter as long as its positive. The AI should be forced to research the specified technology ASAP (if able).
	}
	ai_strategy = {
		type = research_tech
		id = construction2
		value = 50000  # Value doesn't really matter as long as its positive. The AI should be forced to research the specified technology ASAP (if able).
	}
	ai_strategy = {
		type = research_tech
		id = construction3
		value = 50000  # Value doesn't really matter as long as its positive. The AI should be forced to research the specified technology ASAP (if able).
	}
	ai_strategy = {
		type = research_tech
		id = construction4
		value = 50000  # Value doesn't really matter as long as its positive. The AI should be forced to research the specified technology ASAP (if able).
	}
	ai_strategy = {
		type = research_tech
		id = construction5
		value = 50000  # Value doesn't really matter as long as its positive. The AI should be forced to research the specified technology ASAP (if able).
	}
}

spread_them_factories = {
	enable = {
		always = yes
	}
	abort_when_not_enabled = yes


	ai_strategy = {
		type = research_tech
		id = dispersed_industry
		value = 50000  # Value doesn't really matter as long as its positive. The AI should be forced to research the specified technology ASAP (if able).
	}
	ai_strategy = {
		type = research_tech
		id = dispersed_industry2
		value = 50000  # Value doesn't really matter as long as its positive. The AI should be forced to research the specified technology ASAP (if able).
	}
	ai_strategy = {
		type = research_tech
		id = dispersed_industry3
		value = 50000  # Value doesn't really matter as long as its positive. The AI should be forced to research the specified technology ASAP (if able).
	}
	ai_strategy = {
		type = research_tech
		id = dispersed_industry4
		value = 50000  # Value doesn't really matter as long as its positive. The AI should be forced to research the specified technology ASAP (if able).
	}
	ai_strategy = {
		type = research_tech
		id = dispersed_industry5
		value = 50000  # Value doesn't really matter as long as its positive. The AI should be forced to research the specified technology ASAP (if able).
	}
	
	ai_strategy = {
		type = research_weight_factor
		id = dispersed_industry
		value = 800  # Value doesn't really matter as long as its positive. The AI should be forced to research the specified technology ASAP (if able).
	}
	ai_strategy = {
		type = research_weight_factor
		id = dispersed_industry2
		value = 800  # Value doesn't really matter as long as its positive. The AI should be forced to research the specified technology ASAP (if able).
	}
	ai_strategy = {
		type = research_weight_factor
		id = dispersed_industry3
		value = 800  # Value doesn't really matter as long as its positive. The AI should be forced to research the specified technology ASAP (if able).
	}
	ai_strategy = {
		type = research_weight_factor
		id = dispersed_industry4
		value = 800  # Value doesn't really matter as long as its positive. The AI should be forced to research the specified technology ASAP (if able).
	}
	ai_strategy = {
		type = research_weight_factor
		id = dispersed_industry5
		value = 800  # Value doesn't really matter as long as its positive. The AI should be forced to research the specified technology ASAP (if able).
	}
}

big_brained = {
	enable = {
		always = yes
	}
	abort_when_not_enabled = yes


	ai_strategy = {
		type = research_tech
		id = electronic_mechanical_engineering
		value = 50000  # Value doesn't really matter as long as its positive. The AI should be forced to research the specified technology ASAP (if able).
	}
	ai_strategy = {
		type = research_tech
		id = mechanical_computing
		value = 50000  # Value doesn't really matter as long as its positive. The AI should be forced to research the specified technology ASAP (if able).
	}
	ai_strategy = {
		type = research_tech
		id = computing_machine
		value = 50000  # Value doesn't really matter as long as its positive. The AI should be forced to research the specified technology ASAP (if able).
	}
	ai_strategy = {
		type = research_tech
		id = improved_computing_machine
		value = 50000  # Value doesn't really matter as long as its positive. The AI should be forced to research the specified technology ASAP (if able).
	}
	ai_strategy = {
		type = research_tech
		id = advanced_computing_machine
		value = 50000  # Value doesn't really matter as long as its positive. The AI should be forced to research the specified technology ASAP (if able).
	}
}

know_the_tanky = {
	enable = {
		threat > 0.01
		num_of_military_factories > 20
	}
	abort_when_not_enabled = yes
	ai_strategy = {
		type = research_weight_factor
		id = interwar_antitank
		value = 50000  # Value doesn't really matter as long as its positive. The AI should be forced to research the specified technology ASAP (if able).
	}

	ai_strategy = {
		type = research_weight_factor
		id = antitank1
		value = 50000  # Value doesn't really matter as long as its positive. The AI should be forced to research the specified technology ASAP (if able).
	}

	ai_strategy = {
		type = research_weight_factor
		id = antitank2
		value = 500  # Value doesn't really matter as long as its positive. The AI should be forced to research the specified technology ASAP (if able).
	}

	ai_strategy = {
		type = research_weight_factor
		id = antitank3
		value = 500  # Value doesn't really matter as long as its positive. The AI should be forced to research the specified technology ASAP (if able).
	}
	
	ai_strategy = {
		type = research_weight_factor
		id = antitank4
		value = 500  # Value doesn't really matter as long as its positive. The AI should be forced to research the specified technology ASAP (if able).
	}

	ai_strategy = {
		type = research_weight_factor
		id = antitank5
		value = 500  # Value doesn't really matter as long as its positive. The AI should be forced to research the specified technology ASAP (if able).
	}
	
	ai_strategy = {
		type = research_weight_factor
		id = tech_maintenance_company
		value = 5000  # Value doesn't really matter as long as its positive. The AI should be forced to research the specified technology ASAP (if able).
	}
	
	ai_strategy = {
		type = research_weight_factor
		id = tech_maintenance_company2
		value = 5000  # Value doesn't really matter as long as its positive. The AI should be forced to research the specified technology ASAP (if able).
	}

	ai_strategy = {
		type = research_weight_factor
		id = basic_medium_tank
		value = 9000  # Value doesn't really matter as long as its positive. The AI should be forced to research the specified technology ASAP (if able).
	}

	ai_strategy = {
		type = research_weight_factor
		id = basic_medium_tank_chassis
		value = 9000  # Value doesn't really matter as long as its positive. The AI should be forced to research the specified technology ASAP (if able).
	}
	
	ai_strategy = {
		type = research_weight_factor
		id = improved_medium_tank
		value = 9000  # Value doesn't really matter as long as its positive. The AI should be forced to research the specified technology ASAP (if able).
	}

	ai_strategy = {
		type = research_weight_factor
		id = improved_medium_tank_chassis
		value = 9000  # Value doesn't really matter as long as its positive. The AI should be forced to research the specified technology ASAP (if able).
	}

	ai_strategy = {
		type = research_weight_factor
		id = advanced_medium_tank
		value = 9000  # Value doesn't really matter as long as its positive. The AI should be forced to research the specified technology ASAP (if able).
	}

	ai_strategy = {
		type = research_weight_factor
		id = advanced_medium_tank_chassis
		value = 9000  # Value doesn't really matter as long as its positive. The AI should be forced to research the specified technology ASAP (if able).
	}
	
	ai_strategy = {
		type = research_weight_factor
		id = main_battle_tank
		value = 9000  # Value doesn't really matter as long as its positive. The AI should be forced to research the specified technology ASAP (if able).
	}

	ai_strategy = {
		type = research_weight_factor
		id = main_battle_tank_chassis
		value = 9000  # Value doesn't really matter as long as its positive. The AI should be forced to research the specified technology ASAP (if able).
	}
}

know_the_plane = {
	enable = {
		threat > 0.01
		num_of_military_factories > 20
	}
	abort_when_not_enabled = yes

	ai_strategy = {
		type = research_weight_factor
		id = survivability_studies
		value = 700  # Value doesn't really matter as long as its positive. The AI should be forced to research the specified technology ASAP (if able).
	}
	
	ai_strategy = {
		type = research_weight_factor
		id = aa_hmg
		value = 700  # Value doesn't really matter as long as its positive. The AI should be forced to research the specified technology ASAP (if able).
	}

	ai_strategy = {
		type = research_weight_factor
		id = engines_1
		value = 700  # Value doesn't really matter as long as its positive. The AI should be forced to research the specified technology ASAP (if able).
	}
	
	ai_strategy = {
		type = research_weight_factor
		id = engines_2
		value = 700  # Value doesn't really matter as long as its positive. The AI should be forced to research the specified technology ASAP (if able).
	}
	
	ai_strategy = {
		type = research_weight_factor
		id = engines_3
		value = 700  # Value doesn't really matter as long as its positive. The AI should be forced to research the specified technology ASAP (if able).
	}
	
	ai_strategy = {
		type = research_weight_factor
		id = engines_4
		value = 700  # Value doesn't really matter as long as its positive. The AI should be forced to research the specified technology ASAP (if able).
	}
	
	ai_strategy = {
		type = research_weight_factor
		id = range_improvements
		value = 700  # Value doesn't really matter as long as its positive. The AI should be forced to research the specified technology ASAP (if able).
	}
	
	ai_strategy = {
		type = research_weight_factor
		id = early_fighter
		value = 700  # Value doesn't really matter as long as its positive. The AI should be forced to research the specified technology ASAP (if able).
	}
	
	ai_strategy = {
		type = research_weight_factor
		id = fighter1
		value = 3000  # Value doesn't really matter as long as its positive. The AI should be forced to research the specified technology ASAP (if able).
	}
	
	ai_strategy = {
		type = research_weight_factor
		id = fighter2
		value = 3000  # Value doesn't really matter as long as its positive. The AI should be forced to research the specified technology ASAP (if able).
	}

	ai_strategy = {
		type = research_weight_factor
		id = fighter3
		value = 3000  # Value doesn't really matter as long as its positive. The AI should be forced to research the specified technology ASAP (if able).
	}

}

know_the_gun = {
	enable = {
		threat > 0.1
	}
	abort_when_not_enabled = yes

	ai_strategy = {
		type = research_weight_factor
		id = infantry_weapons
		value = 700  # Value doesn't really matter as long as its positive. The AI should be forced to research the specified technology ASAP (if able).
	}
	
	ai_strategy = {
		type = research_weight_factor
		id = infantry_weapons1
		value = 700  # Value doesn't really matter as long as its positive. The AI should be forced to research the specified technology ASAP (if able).
	}

	ai_strategy = {
		type = research_weight_factor
		id = improved_infantry_weapons
		value = 700  # Value doesn't really matter as long as its positive. The AI should be forced to research the specified technology ASAP (if able).
	}
	
	ai_strategy = {
		type = research_weight_factor
		id = improved_infantry_weapons_2
		value = 700  # Value doesn't really matter as long as its positive. The AI should be forced to research the specified technology ASAP (if able).
	}
	
	ai_strategy = {
		type = research_weight_factor
		id = infantry_at
		value = 700  # Value doesn't really matter as long as its positive. The AI should be forced to research the specified technology ASAP (if able).
	}
	
	ai_strategy = {
		type = research_weight_factor
		id = infantry_at2
		value = 700  # Value doesn't really matter as long as its positive. The AI should be forced to research the specified technology ASAP (if able).
	}
	
	ai_strategy = {
		type = research_weight_factor
		id = advanced_infantry_weapons
		value = 700  # Value doesn't really matter as long as its positive. The AI should be forced to research the specified technology ASAP (if able).
	}
	
	ai_strategy = {
		type = research_weight_factor
		id = advanced_infantry_weapons2
		value = 700  # Value doesn't really matter as long as its positive. The AI should be forced to research the specified technology ASAP (if able).
	}
	
	ai_strategy = {
		type = research_weight_factor
		id = marines
		value = 700  # Value doesn't really matter as long as its positive. The AI should be forced to research the specified technology ASAP (if able).
	}
	
	ai_strategy = {
		type = research_weight_factor
		id = support_weapons
		value = 700  # Value doesn't really matter as long as its positive. The AI should be forced to research the specified technology ASAP (if able).
	}
	
	ai_strategy = {
		type = research_weight_factor
		id = support_weapons2
		value = 700  # Value doesn't really matter as long as its positive. The AI should be forced to research the specified technology ASAP (if able).
	}
	
	ai_strategy = {
		type = research_weight_factor
		id = support_weapons3
		value = 700  # Value doesn't really matter as long as its positive. The AI should be forced to research the specified technology ASAP (if able).
	}
	
	ai_strategy = {
		type = research_weight_factor
		id = support_weapons4
		value = 700  # Value doesn't really matter as long as its positive. The AI should be forced to research the specified technology ASAP (if able).
	}
}

know_the_nuke = {
	enable = {
		threat > 0.6
		is_major = yes
	}
	abort_when_not_enabled = yes

	ai_strategy = {
		type = research_tech
		id = atomic_research
		value = 10000  # Value doesn't really matter as long as its positive. The AI should be forced to research the specified technology ASAP (if able).
	}
	

}