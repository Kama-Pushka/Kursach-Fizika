# befriend
# conquer
# antagonize
# build_ship
# build_army
# unit_ratio
# build_building
# research_tech
# garrison
# protect
# influence


COG_help_ETH_win_civil_war = {
	reversed = yes
	enable = {
		date < 1939.1.1
		original_tag = ETH
		ETH = { has_war_with = ITA }
		NOT = { has_global_flag = italo_ethiopian_war_over }
	}
	abort = {
		OR = {
			date > 1939.1.1
			ETH = { NOT = { has_war_with = ITA } }
			has_global_flag = italo_ethiopian_war_over
		}
	}
	
	ai_strategy = {
		type = send_volunteers_desire
		id = COG
		value = 100
	}
}




