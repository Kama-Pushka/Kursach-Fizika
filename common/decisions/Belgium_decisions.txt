BEL_integrate_category = {

	BEL_integrate_hol_fla = {

		icon = generic_form_nation
		
		available = {
			all_state = {
				OR = {
					NOT = { is_core_of = HOL }
					controller = {
						OR = {
							tag = ROOT
							is_subject_of = ROOT
						}
					}
				}
			}
		}
		
		visible = {
			tag = BEL
			#FLA = { is_subject_of = ROOT }
			#has_country_flag = BEL_integrate_category_flag
		}
		
		cost = 100
		fire_only_once = yes

		complete_effect = {
			every_state = {
				limit = { is_core_of = HOL }
				add_core_of = FLA
			}
		}

		ai_will_do = {
			factor = 100
		}
	}
	
	BEL_integrate_hol_bel = {

		icon = generic_form_nation
		
		available = {
			all_state = {
				OR = {
					NOT = { is_core_of = HOL }
					controller = {
						OR = {
							tag = ROOT
							is_subject_of = ROOT
						}
					}
				}
			}
		}
		
		visible = {
			tag = BEL
			#NOT = { FLA = { is_subject_of = ROOT } }
			#has_country_flag = BEL_integrate_category_flag
		}
		
		cost = 200
		fire_only_once = yes

		complete_effect = {
			every_state = {
				limit = { is_core_of = HOL }
				add_core_of = ROOT
			}
		}

		ai_will_do = {
			factor = 100
		}
	}
}



