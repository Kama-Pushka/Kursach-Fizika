POR_unions_des_cat = {
	POR_union_with_spanish = {
		icon = decision_hol_exchange_intelligence_data
		allowed = { tag = POR }
		cost = 25
		available = {
			has_political_power > 25
			OR = {
				AND = {
					OR = {
						has_government = neutrality
						has_government = monarchy
						has_government = fascism
					}
					SPA = { has_government = fascism }
					NOT = { country_exists = SPR }
				}
				AND = {
					has_government = communism
					SPR = { has_government = communism }
					NOT = { country_exists = SPA }
				}
			}
			NOT = {
				OR = {
					is_in_faction_with = SPA
					is_in_faction_with = SPR
				}
			}
		}
		remove_effect = {
			if = { limit = { country_exists = SPA } SPA = { country_event = { id = portugal.6 days = 1 } } }
			if = { limit = { country_exists = SPR } SPR = { country_event = { id = portugal.6 days = 1 } } }
		}
		ai_will_do = {
			factor = 0
		}
		days_remove = 30
		fire_only_once = yes
	}
}