prestigious_projects = {
	HAI_mission_railways_reorganisation_program = {
		icon = generic_construction
		activation = {
			has_completed_focus = HAI_recreation_transport_system
		}
		available = {
			318 = {
				infrastructure > 2
			}
			869 = {
				infrastructure > 2
			}
			num_of_civilian_factories > 4
		}
		allowed = { tag = HAI }
		is_good = yes
		days_mission_timeout = 340
		complete_effect = { 
			add_stability = 0.07
			318 = {
				add_extra_state_shared_building_slots = 2
				add_building_construction = {
					type = industrial_complex
					level = 1
					instant_build = yes
				}
			}
		}
		timeout_effect = {
			add_popularity = {
				ideology = neutrality
				popularity = -0.1
			}
			add_political_power = -95
		}
	}
}

crisis = {
	DOMHAI_petrushke_pizda = {
		icon = generic_assassination
		activation = {
			date > "1936.1.1"
		}
		available = {
			always = no
		}
		fire_only_once = yes
		allowed = { OR = { tag = HAI tag = DOM } }
		is_good = no
		days_mission_timeout = 639 
		timeout_effect = {
			if = {
				limit = {
					tag = HAI
				}
				country_event = { id = haiti.1 }
			}
			set_global_flag = HAIDOM_massacre
		}
	}
	HAI_calixte_coup = {
		icon = generic_reorganize_irregulars
		fire_only_once = yes
		activation = {
			always = no
		}
		available = {
			OR = { has_completed_focus = HAI_deploy_border_forces has_completed_focus = HAI_arest_KALixte }
		}
		allowed = { tag = HAI }
		is_good = no
		days_mission_timeout = 270
		timeout_effect = {
			country_event = { id = haiti.14 days = 1 }
		}
		complete_effect = { 
			add_stability = 0.045
			add_war_support = 0.045
		}
	}
	HAI_vensane_reaction = {
		fire_only_once = yes
		activation = {
			always = no
		}
		available = {
			OR = { has_completed_focus = HAI_propaganda_against_trujilio has_completed_focus = HAI_stop_press }
		}
		allowed = { tag = HAI }
		is_good = no
		days_mission_timeout = 16
		timeout_effect = {
			add_political_power = -60
			add_stability = -0.045
			add_days_mission_timeout = {
				mission = HAI_calixte_coup
				days = -25
			}
		}
		complete_effect = { 
			
		}
	}
	HAI_second_try = {
		fire_only_once = yes
		activation = {
			always = no
		}
		available = {
			OR = { has_completed_focus = HAI_dominican_compensation has_completed_focus = HAI_deploy_border_forces }
		}
		allowed = { tag = HAI }
		is_good = no
		days_mission_timeout = 16
		timeout_effect = {
			add_political_power = -60
			add_stability = -0.045
			add_days_mission_timeout = {
				mission = HAI_calixte_coup
				days = -25
			}
		}
		complete_effect = { 
			
		}
	}
	HAI_finaly_we_will_end_this_shit = {
		
		allowed = {
			original_tag = HAI
		}

		available = {
			NOT = { has_country_flag = HAI_timer_petru }
		}
		
		visible = {
			OR = { has_completed_focus = HAI_dominican_compensation has_completed_focus = HAI_deploy_border_forces }
		}
	
		cost = 165
		fire_only_once = yes
		
		complete_effect = {
			if = {
				limit = { has_idea = HAI_reznyaaa_r_1 }
				remove_ideas = HAI_reznyaaa_r_1
			}
			else = {
				remove_ideas = HAI_reznyaaa_l_1
			}
			add_stability = 0.065
		}
		
		ai_will_do = {
			factor = 200
		}
	}
	HAI_border_conflict = {
		fire_only_once = yes
		activation = {
			always = no
		}
		available = {
			OR = { has_completed_focus = HAI_we_will_never_surrender }
		}
		allowed = { tag = HAI }
		is_good = no
		days_mission_timeout = 70
		timeout_effect = {
			add_political_power = -60
			add_stability = -0.045
			add_war_support = -0.08
		}
		complete_effect = { 
			
		}
	}
}