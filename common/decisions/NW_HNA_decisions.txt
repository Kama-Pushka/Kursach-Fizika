HNA_hunan_policy_category = {
	
	HNA_hunan_policy = {
		icon = GFX_decision_generic_arrest

		available = {
			has_war = yes
			is_subject = no
			controls_state = 602
			has_manpower > 4000
		}
		
		cost = 100
		
		days_remove = 30
		# fire_only_once = no

		complete_effect = {
			log = "[GetDateText]: [Root.GetName] ([Root.GetTag]): complete decision HNA_hunan_policy"
			hidden_effect = {
				if = {
					limit = { NOT = { has_template = "Hunan Militia" } }
					division_template = {
						name = "Hunan Militia"
						is_locked = yes
						regiments = {
							militia = { x = 0 y = 0 }
							militia = { x = 0 y = 1 }
							militia = { x = 0 y = 2 }
							militia = { x = 1 y = 0 }
						}
						support = {
							artillery = { x = 0 y = 0 }
						}
					}
				}
			}
		}

		remove_effect = {
			log = "[GetDateText]: [Root.GetName] ([Root.GetTag]): remove decision HNA_hunan_policy"
			add_manpower = -4000
			add_to_variable = { HNA_hunan_militia_battalions = 1 }
			capital_scope = {
				create_unit = {
					division = "division_template = \"Hunan Militia\" start_experience_factor = 0.4"
					owner = ROOT
					count = 1
				}
			}
		}
		
		ai_will_do {
			factor = 10
		}
	}
}

HNA_hunan_mission_category = {

	HNA_hunan_army_support = {
		icon = GFX_decision_generic_arrest

		available = {
			is_subject = no
		}

		custom_cost_trigger = {
			set_temp_variable = { custom_cost_pp = 100 }
			set_temp_variable = { command_power_temp_cost = 25 }
			check_variable = { has_political_power > custom_cost_pp }
			check_variable = { command_power > command_power_temp_cost }
		}
		custom_cost_text = pol_and_comnand_power_cost # TODO: поставить общий
		
		fire_only_once = yes

		complete_effect = {
			log = "[GetDateText]: [Root.GetName] ([Root.GetTag]): complete decision HNA_hunan_army_support"
			hidden_effect = {
				add_political_power = -100
				add_command_power = -25
			}
			add_popularity = {
				ideology = fascism
				popularity = 0.03
			}
			add_to_variable = { HNA_hunan_army_support_decision = 1 }
		}
		
		ai_will_do {
			factor = 10
		}
	}

	HNA_hunan_army_ideology = {
		icon = GFX_decision_generic_arrest

		available = {
			is_subject = no
		}

		custom_cost_trigger = {
			set_temp_variable = { custom_cost_pp = 25 }
			set_temp_variable = { command_power_temp_cost = 5 }
			has_political_power > custom_cost_pp
			command_power > command_power_temp_cost
		}
		custom_cost_text = pol_and_comnand_power_cost
		
		days_remove = 120
		fire_only_once = no

		complete_effect = {
			log = "[GetDateText]: [Root.GetName] ([Root.GetTag]): complete decision HNA_hunan_army_ideology"
			hidden_effect = {
				add_political_power = -25
				add_command_power = -5
			}
			add_popularity = {
				ideology = fascism
				popularity = 0.015
			}
		}
		
		ai_will_do {
			factor = 10
		}
	}

	HNA_hunan_government_support = {
		icon = GFX_decision_generic_arrest
		
		available = {
			is_subject = no
		}
		
		cost = 200
		fire_only_once = yes
		
		complete_effect = {
			log = "[GetDateText]: [Root.GetName] ([Root.GetTag]): complete decision HNA_hunan_government_support"
			add_popularity = {
				ideology = fascism
				popularity = 0.03
			}
			add_stability = -0.05
			add_to_variable = { HNA_hunan_government_support_decision = 1 }
		}
		
		ai_will_do {
			factor = 10
		}
	}

	HNA_hunan_missioners = {
		icon = GFX_decision_generic_arrest
		
		available = {
			is_subject = no
		}
		
		cost = 50
		# fire_only_once = no
		days_remove = 120
		
		complete_effect = {
			log = "[GetDateText]: [Root.GetName] ([Root.GetTag]): complete decision HNA_hunan_missioners"
			add_popularity = {
				ideology = fascism
				popularity = 0.015
			}
		}
		
		ai_will_do {
			factor = 10
		}
	}

	HNA_hunan_join_government = {
		icon = GFX_decision_generic_arrest

		available = {
			country_exists = SIH
			SIH = { is_subject = no }
		}
		
		cost = 50
		fire_only_once = yes

		complete_effect = {
			log = "[GetDateText]: [Root.GetName] ([Root.GetTag]): complete decision HNA_hunan_join_government"
			if = {
				limit = { 
					fascism > 0.64
					is_subject = yes
				}
				SIH = {
					set_autonomy = {
						target = HNA
						autonomy_state = autonomy_dominion
					}
					country_event = { id = HNA.4 }
				}
			}
			else_if = {
				limit = { 
					fascism > 0.64
				}
				SIH = {
					set_autonomy = {
						target = HNA
						autonomy_state = autonomy_dominion
					}
				}
			}
			else_if = {
				limit = { 
					fascism > 0.3
					check_variable = { 
						var = HNA_hunan_government_support_decision 
						value = 1 
						compare = greater_than_or_equals 
					}
					check_variable = { 
						var = HNA_hunan_army_support_decision 
						value = 1 
						compare = greater_than_or_equals 
					}
					is_subject = yes
				}
				SIH = {
					set_autonomy = {
						target = HNA
						autonomy_state = autonomy_dominion
					}
					country_event = { id = HNA.4 }
				}
			}
			else_if = {
				limit = { 
					fascism > 0.3
					check_variable = { 
						var = HNA_hunan_government_support_decision 
						value = 1 
						compare = greater_than_or_equals 
					}
					check_variable = { 
						var = HNA_hunan_army_support_decision 
						value = 1 
						compare = greater_than_or_equals 
					}
				}
				SIH = {
					set_autonomy = {
						target = HNA
						autonomy_state = autonomy_dominion
					}
				}
				country_event = HNA.5
			}
			else_if = {
				limit = { is_subject = yes }
				country_event = HNA.6
			}
			else = {
				country_event = HNA.35
			}
		}
		
		ai_will_do {
			factor = 10
		}
	}
}

HNA_rebellion_xiangxi_category = {

	HNA_intensification_of_rebellion = {
		icon = GFX_decision_generic_arrest
		
		allowed = {
			always = no
		}
		activation = { 
			always = no
		}
		available = {
			NOT = { has_country_flag = HNA_rebellion_xiangxi_flag }
		}

		is_good = no
		days_mission_timeout = 365

		fire_only_once = yes

		complete_effect = {
			log = "[GetDateText]: [Root.GetName] ([Root.GetTag]): complete decision HNA_hunan_join_government"
		}

		timeout_effect = {
			log = "[GetDateText]: [Root.GetName] ([Root.GetTag]): timeout decision HNA_hunan_join_government"
			if = {
				limit = { is_subject = yes }
				OVERLORD = { country_event = HNA.16 }
			}
			else_if = {
				limit = { 
					check_variable = { 
						var = HNA_has_anti_jap_event 
						value = 1 
						compare = greater_than_or_equals 
					}
				}
				country_event = { id = HNA.22 }
			}
			else = {
				country_event = { id = HNA.23 }
			} 
		}
	}

	HNA_insurrection = { # TODO activate_targeted_decision
		icon = GFX_decision_generic_arrest

		allowed = {
			always = no
		}
		activation = {
			always = no
		}
		available = {
			NOT = { 
				has_country_flag = HNA_rebellion_xiangxi_flag 
				controls_state = 750
			}
		}
		
		is_good = no
		days_mission_timeout = 90
		
		fire_only_once = no
		
		complete_effect = {
			log = "[GetDateText]: [Root.GetName] ([Root.GetTag]): complete decision HNA_insurrection"
		}

		timeout_effect = {
			log = "[GetDateText]: [Root.GetName] ([Root.GetTag]): timeout decision HNA_insurrection"
			750 = {
				random_list = {
					25 = { 
						damage_building = {
							type = industrial_complex
							damage = 2
						}
					} 
					25 = { 
						damage_building = {
							type = infrastructure
							damage = 2
						}
					} 
					25 = {
						damage_building = {
							type = arms_factory
							damage = 2
						}
					}
					25 = { }
				}
			}
			activate_mission = HNA_insurrection
		}
	}

	HNA_interrogate_chen_quzhen = {
		icon = GFX_decision_generic_arrest
		
		cost = 20
		fire_only_once = yes
		
		complete_effect = {
			log = "[GetDateText]: [Root.GetName] ([Root.GetTag]): complete decision HNA_interrogate_chen_quzhen"
			country_event = HNA.20
		}
		
		ai_will_do {
			factor = 10
		}
	}
	
	HNA_cancel_tongtian_system = {
		icon = GFX_decision_generic_arrest
		
		cost = 150
		fire_only_once = yes
		days_remove = 30
		
		complete_effect = {
			log = "[GetDateText]: [Root.GetName] ([Root.GetTag]): complete decision HNA_cancel_tongtian_system"
			country_event = { id = HNA.26 }
		}
		
		ai_will_do {
			factor = 10
		}
	}
	
	HNA_start_suppression = {
		icon = GFX_decision_generic_arrest

		visible = {
			NOT = { has_active_mission = HNA_suppression_the_rebels }
		}

		available = {
			has_army_manpower = { size > 99999 }
			divisions_in_state = { state = 750 size > 4 }
		}
		
		fire_only_once = no
		
		complete_effect = {
			log = "[GetDateText]: [Root.GetName] ([Root.GetTag]): complete decision HNA_start_suppression"
			activate_mission = HNA_suppression_the_rebels
		}
		
		ai_will_do {
			factor = 10
		}
	}
	HNA_suppression_the_rebels = {
		icon = GFX_decision_generic_arrest
		
		allowed = {
			always = no
		}
		activation = {
			always = no
		}
		available = {
			divisions_in_state = { state = 750 size > 4 }
		}

		days_mission_timeout = 150

		# cancel_trigger = { TODO
			# NOT = {
				# check_variable = { 
					# var = HNA_suppression_the_rebels
					# value = 1 
					# compare = greater_than_or_equals 
				# }
			# }
		# }

		is_good = yes

		complete_effect = { # FAIL
			log = "[GetDateText]: [Root.GetName] ([Root.GetTag]): complete decision HNA_suppression_the_rebels"
			country_event = { id = HNA.24 }
		}

		timeout_effect = { # SUCCESS
			log = "[GetDateText]: [Root.GetName] ([Root.GetTag]): timeout decision HNA_suppression_the_rebels"
			country_event = { id = HNA.25 }
		}
	}
}