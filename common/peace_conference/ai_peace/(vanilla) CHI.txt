# Scripted AI desire for peace actions.
# See documentation.info file for more detailed scripting information.

peace_ai_desires = {

    # Try to grab Chinese states for yourself 
    PRC_take_chinese_cores_if_possible = { # TODO
        peace_action_type = take_states
        enable = {
            FROM.FROM.FROM = {
                is_core_of = PRC
            }
            ROOT = { original_tag = PRC }
        }
        ai_desire = 150
    }

    # Try to grab Chinese states for yourself 
    CHI_take_chinese_cores_if_possible = { # TODO
        peace_action_type = take_states
        enable = {
            FROM.FROM.FROM = {
                is_core_of = CHI
            }
            ROOT = { original_tag = CHI }
        }
        ai_desire = 150
    }

}
